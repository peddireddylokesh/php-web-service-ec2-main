<?php
// This file is intentionally left mostly empty
// It ensures the src directory exists for the autoloader

namespace App;

// Define application constants
define('APP_VERSION', '1.0.0');
define('APP_ENV', getenv('APP_ENV') ?: 'development');
